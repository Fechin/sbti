interface:
  display_name: "SBTI"
  short_description: "Run the SBTI personality test"
  default_prompt: "Run the SBTI personality test in the user's preferred language and summarize the result clearly."
