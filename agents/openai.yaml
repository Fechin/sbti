interface:
  display_name: "SBTI"
  short_description: "Run the self-contained SBTI personality test"
  default_prompt: "Run the portable SBTI personality test from the bundled skill resources under skills/sbti. Prefer the local Python script and bundled JSON data; if script execution is unavailable, use the manual workflow to ask questions, score the 15 dimensions, and summarize the final SBTI personality type and top matches clearly."
